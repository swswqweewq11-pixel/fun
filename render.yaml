services:
  - type: web
    name: roblox-headshot-proxy-py
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: CACHE_TTL
        value: "300"
